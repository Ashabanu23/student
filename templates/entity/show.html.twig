{% extends 'base.html.twig' %}

{% block title %}Entity{% endblock %}

{% block body %}
     <div class="container">
    
    <div class="row">
    <div class="col-md-12">
      <div class="card bg-light mb-3 mt-3">
        <div class="card-body">
          <div class="card-header"><img src="/student/public/img/food-item.png" width="50px" height="50px" /><span style="color:blue; font-size:25px;">&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; Food Item</span></div>
          <AmpContent>
        <br>


    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ entity.price }}</td>
            </tr>
            <tr>
                <th>Qty</th>
                <td>{{ entity.qty }}</td>
            </tr>
			 <tr>
                <th>Category</th>
                <td>{{ entity.category.name}}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('entity_index') }}">Back to list</a>
    <br>
   <!-- <a href="{{ path('entity_edit', {'id': entity.id}) }}"><span color:blue;>Edit</span></a>-->
    <br>
    {{ include('entity/_delete_form.html.twig') }}
	
</NonAmpContent>
        </div>
      </div>
      <AmpContent>
   
      
</NonAmpContent>
    </div>
  </div>
</div>
{% endblock %}
